services:
  app:
    container_name: rust-ds
    image: rust:slim
    restart: unless-stopped
    environment:
      - DEEPSEEK_API_KEY=$DEEPSEEK_API_KEY
    volumes:
      - .:/app
      - .git:/app/.git:ro
    working_dir: /app
    command: ["sleep", "infinity"]
