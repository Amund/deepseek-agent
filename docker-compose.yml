services:
  app:
    container_name: deepseek-agent
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./workspace:/app
      - ./target/release/deepseek-agent:/usr/local/bin/deepseek
    working_dir: /app
    command: sleep infinity
